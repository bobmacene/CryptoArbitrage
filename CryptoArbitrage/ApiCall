
using System.Net;

namespace CryptoArbitrage
{
    public interface iApiCall
    {
        string CallApi(string url);
    }
    class ExchangeCurrencyPairPrice : iApiCall
    {
        public string CallApi(string url)
        {
            return new WebClient().DownloadString(url);
        }

    }
}
